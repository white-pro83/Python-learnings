entrada = "PyThOn"

# Convertir a mayusculas
grito = entrada.upper()
print(grito) #Salida: "PYTHON"

# Convertir a minusculas (Estandarizar)
normal = entrada.lower()
print(normal) # Salida: "python"

# La original sigue intacta
print(entrada) # Salida: "PyThOn"